defmodule Demo do
  @moduledoc """
  Demo document of whole neuron.
  """

  #alias Glowworm.Neuron
  #alias Glowworm.SomaRunner, as: Soma
  #alias Glowworm.SynapseRunner, as: Synapse

  def action(), do: nil
end

defmodule SimulusRecord do
  @moduledoc """
  """

  def loop(), do: nil
end

# spawn(Demo, :action, [])
